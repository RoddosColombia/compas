# COMPAS backend — dependencias de runtime (Python 3.12 en Render)
# Pinneadas a versiones verificadas localmente. Stack: STACK v1.1.2 §2.
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.15.0
beanie==2.0.0
motor==3.7.1
anyio==4.14.2
httpx==0.28.1
openpyxl==3.1.5
pdfplumber==0.11.10  # E2: extracción del IVA de la Representación Gráfica DIAN (PDF). 0.11.9 (no 0.11.4) porque su pin pdfminer.six==20251230 cierra PYSEC-2026-1761/1762 (pip-audit); es además la versión en que se validó el extractor (A1)
python-multipart==0.0.31  # UploadFile (POST /cargas); FastAPI lo exige en runtime
APScheduler==3.11.3
bcrypt==5.0.0
PyJWT==2.13.0
pyotp==2.10.0
cryptography==50.0.0  # CVE-2026-69247/69248/69249 (fix del advisory 2026-08-03; pdfminer.six >=36 compat)
sentry-sdk>=2.66.1,<3.0
boto3==1.43.62  # PR-S3 (DISP-02/M-04): preserva el original de cada extracto en S3 con Object Lock
anthropic==0.100.0  # FABS inc2: SDK del LLM del agente CFO (import perezoso en ClienteAnthropic; solo runtime, sin llamadas en import)
