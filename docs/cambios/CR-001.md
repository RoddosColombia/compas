# CR-001 — Gestión de extractos mensuales (EXHIBIT firmado)

> **Estado:** APROBADO Y FIRMADO · **Solicitante:** CEO Andrés Sanjuan · **Fecha:** 18-jul-2026
> (post-certificado GO v1.1.2). Registrado en el tracker `docs/COMPAS_Control_Desarrollo.xlsx`
> (hoja CRs, fila 2: "Aprobado y firmado"). Fuente normativa completa:
> `docs/COMPAS_CR-001_Extractos.docx` → extracción versionada en `docs/md/COMPAS_CR-001_Extractos.md`.
>
> **Propósito de este exhibit** (condición 1 de la auditoría Kimi R-PLAN): dejar el CR-001 en el
> expediente del repo, citable, para que el catálogo de auditoría = **30 eventos** deje de ser
> testimonio y sea evidencia. `extracto.cargado` es el evento nº 30.

## Puntos normativos que este CR fija (relevantes para la Sesión 2)

- **Catálogo de auditoría pasa de 29 a 30** (CR-001 §2.1, campo `cargado_por` de `ExtractoMensual`:
  *"evento nuevo: extracto.cargado — catálogo pasa de 29 a 30"*).
- **`extracto.cargado` ≠ `carga.completada`** — son sujetos distintos:
  - `extracto.cargado` = carga del **extracto mensual oficial** de un banco (entidad `ExtractoMensual`),
    al cerrar el período, para verificar/corregir cifras. **Emisor: `POST /extractos`** (CR-001 §2.3).
    **Sprint 4** (conciliación).
  - `carga.completada` = carga **diaria** de movimientos (M7, cadencia 8:30).
- **Documentos afectados** (CR-001 §metadata): PRD M4/M7 y Spec (entidad `ExtractoMensual` + 2 endpoints)
  *"se incorporan como v1.2 en el primer re-baseline natural; mientras tanto este CR-001 es el
  documento normativo del cambio"*. → De ahí que la reconciliación 29→30 en los `.docx` sea errata
  diferida (CR-002), no un bloqueo.

## Firma

| Aprobación | Persona | Fecha |
|---|---|---|
| CEO (solicitante) | Andrés Sanjuan | 18-jul-2026 |

El contenido íntegro (entidad `ExtractoMensual`, verificación automática, endpoints, permisos,
justificación de impacto) está en `docs/md/COMPAS_CR-001_Extractos.md` y en el `.docx` certificado.
