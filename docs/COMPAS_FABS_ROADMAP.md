# FABS — Roadmap de desarrollo (artefacto vivo)

> **Qué es:** el artefacto de control ÚNICO del avance de FABS (agente CFO de RODDOS).
> Muestra la evolución fase a fase en el tiempo, no solo la foto de hoy. **Manda sobre
> cualquier presentación o correo:** si algo lo contradice, gana este archivo.
>
> **Mecánica de actualización (regla del CEO 2026-08-10):** se actualiza **tan pronto
> cierra cada desarrollo** (no al final del incremento). Cada cambio queda **fechado** en
> el Registro de cambios (§4). Responsable de revisarlo: Claude, en cada cierre de pieza.
>
> **Gobierno:** FABS es un módulo interno de COMPAS (`backend/app/cfo/`) detrás del flag
> `CFO_ENABLED` (apagado). Todo lo que toca lectura/cálculo de plata va con gate Kimi.
> Decisiones fundacionales en memoria `fabs-fundacion-decisiones` y en los specs de
> `docs/superpowers/specs/`. Reglas innegociables: las de `CLAUDE.md`.

## 1. Norte de FABS (una línea)

Analista financiero de IA que responde con **cifras reales y trazables** desde COMPAS,
vigila la caja y prepara el Comité de Pagos — **sin ejecutar operaciones y sin inventar
ni una cifra** (el modelo nunca calcula; COMPAS calcula, FABS narra con evidencia).

## 2. Fases / incrementos

Estado: ⬜ Pendiente · 🟡 En curso · ✅ Hecho · 🔒 Bloqueado

| # | Incremento | Qué entrega | Gate | Estado |
|---|---|---|---|---|
| **1** | **Cimiento determinista** (sin LLM) | `app/cfo/calc` (3 conceptos: caja hoy · runway · IVA cuatrimestre, cada cifra con evidencia) + arnés de goldens (`cfo/goldens`) + salvaguarda S1 + flag `CFO_ENABLED`. Motor COMPAS cero diffs. | Kimi (gate-waiver GO CEO 2026-08-11; retroactivo pendiente) | ✅ **MERGEADO a main** (`248bfed`) |
| **2** | **Loop del agente + cifra→evidencia** | Loop con SDK Anthropic (temp 0.1, límites), verificador cifra→evidencia invocado antes de publicar, endpoint `/api/v1/cfo` bajo flag, salida tipada. Primeros eventos `cfo.*` (CR). | Kimi (crítico): **gate-waiver GO CEO 2026-08-17; Kimi retroactivo pendiente** | ✅ **MERGEADO a main** (`7a8bd07`) |
| **3** | **Canal Telegram + piloto Q&A** | Partido en 2 piezas (decisión CEO 2026-08-17). **Pieza A — Verificación por concepto** (prerrequisito de seguridad: el modelo cita `[[concepto]]`, no ve valores, no puede mal-etiquetar; cierra el deferral vinculante de inc2). **Pieza B —** Webhook Telegram, vínculo `telegram_id↔user_id` (allowlist admin), hilos por usuario (`cfo_hilos`), piloto (CEO/CGO/CFO). | Kimi diseño 9.3 GO (A) · código: gate-waiver GO CEO 2026-08-17, Kimi retroactivo pendiente · G2 (B) | ✅ **Hecho** — Pieza A + Pieza B en main, gates Kimi 9.5 |
| **4** | **Cerebro analítico + Vigilante/Comité de Pagos** | **Rebanada 1 (2026-08-23, "el caso de la bodega"): what-if de escenarios** — impacto de un gasto/ingreso hipotético en la caja proyectada, mes de quiebre del umbral, y solver de cuántas motos extra por mes evitan cruzarlo — sobre el pipeline COMPLETO (motor→E1→D2), `motor.py` intocable. **MERGEADA a main (`31684e6`) y EN VIVO** — ver Registro de cambios. **Rebanada 2 (2026-08-29, what-if de palancas de crédito): plazo/cuota inicial/cuota semanal** sobre uno o todos los modelos de moto — mismo pipeline COMPLETO, mismo aislamiento S1, `motor.py` intocable. **CONSTRUIDA (SDD, 5 tareas) en rama `feat/fabs-inc4-rebanada2-palancas`, sin mergear** — ver Registro de cambios. Además (sin construir aún): Jobs `cfo_*` en el Worker (alertas por umbral, paquete del lunes 7:00, cierre mensual comentado) — borrador con liberación humana. | Rebanada 1: Kimi diseño+código (**gate-waiver GO CEO 2026-08-23, retroactivos pendientes**) · Rebanada 2: Kimi diseño+código (**gate-waiver GO CEO 2026-08-29, Kimi ausente ~semanas, retroactivo pendiente — NUNCA simulado**) · Vigilante: G3 (piloto→operación) | 🟢 Rebanada 1 EN VIVO (mergeada `31684e6` 2026-08-26; smoke del CEO sano — impacto y "cuántas motos" reconcilian); 🟡 Rebanada 2 CONSTRUIDA, pendiente merge (flag `CFO_ENABLED` ya está ENCENDIDO en prod desde rebanada 1 ⇒ mergear activa `simular_palanca` de inmediato — decisión de release con el CEO antes de mergear, no gate-waiver automático); resto del cerebro analítico + Vigilante pendientes |
| **5** | **Chat embebido en COMPAS** | Panel de chat en la app (SSE), mismo hilo por usuario que Telegram, RBAC de COMPAS. | — | ⬜ Pendiente |
| **6** | **Evolución** (post-estabilización) | Provisión de IVA como tesorería, proyecciones de escenario conversacionales, Teams, reporte inversionistas. | — | ⬜ Pendiente |

## 3. Gates y prerrequisitos

| Gate | Cuándo | Debe cumplirse |
|---|---|---|
| Kimi (por incremento crítico) | antes de cada merge que toque cálculo/lectura de plata | nota ≥ 9.0 + GO CEO; si Kimi ausente, gate-waiver del CEO + auditoría retroactiva |
| G2 — núcleo confiable | cierre incremento 3 | evals ≥ objetivos; 0 cifras sin evidencia; 0 fallos de parseo; determinismo verificado |
| G3 — piloto→operación | ventana 4-5 semanas (incremento 4) | 2 lunes de Comité reales sin corrección de cifras + 1 cierre validado por Fabián; ≤3 correcciones, ninguna >0,5% del flujo |

**Prerrequisitos / dependencias externas:**
- **Datos frescos (Liz):** cargas al día (movs+caja diario, cartera mensual, facturas).
- **Golden set completo (Fabián):** los 240+60 casos con respuesta a mano — incremento ≥3 (el incremento 1 trae solo el arnés + semilla).
- **LoanTape (SISMO-V3):** para cartera fina; hoy `loantape_creditos` vacía. Ligado a CR-PTS6F.
- **Sunset del CFO legado de SISMO-V2** (D1): higiene previa; el flag de FABS no apaga al legado.
- **Presupuesto operativo:** $30 USD/mes (aplica desde el incremento 2, cuando entra el LLM).

## 4. Registro de cambios (fechado, append-only)

| Fecha | Incremento | Qué cerró / cambió | Evidencia |
|---|---|---|---|
| 2026-08-10 | 1 | Spec del incremento 1 aprobado por el CEO | `docs/superpowers/specs/2026-08-10-fabs-cimiento-determinista-design.md` (commit 898d3c9) |
| 2026-08-10 | 1 | Plan de implementación escrito + roadmap creado | `docs/superpowers/plans/2026-08-10-fabs-cimiento-determinista.md` |
| 2026-08-11 | 1 | **Incremento 1 CONSTRUIDO** (SDD, 11 tasks, subagente+review por tarea). `app/cfo/`: evidencia · flag · 3 conceptos · refactor DRY `liquidacion_iva()` · modelo/runner/semilla de goldens · guard S1. Suite COMPAS **940 passed / 95 skipped**, flag apagado ⇒ idéntico; `motor.py` cero diffs. | rama `feat/fabs-inc1`, commits `63f8ef3..bbe2c3b` |
| 2026-08-11 | 1 | Semilla real de goldens desde PROD (snapshot): caja_hoy 704.722.003 · runway abstención · IVA C2-2026 36.204.698,10 (DIAN 10-sep) | `app/cfo/goldens/semilla.py` (`bbe2c3b`) |
| 2026-08-11 | 1 | **MERGEADO a main** (ff `79ce281..248bfed`). Merge con lo que la terminal avanzó en paralelo (C2' Excel DIAN, Auteco 2 NITs, saldo a favor declarado): conflicto solo en el refactor DRY → resuelto preservando el código de plata de la terminal; `service.liquidacion_iva()` en paridad con el endpoint (incl. saldo declarado) para que FABS lea la cifra oficial. Suite completa **955 passed / 95 skipped**. Gate: **gate-waiver GO CEO 2026-08-11**; Kimi retroactivo pendiente (paquete en `planning/phases/fabs-inc1/auditorias/PR1-I/`). | `248bfed` |
| — | 1→2 | **Deuda de cleanup (inc2):** duplicación endpoint router + `service.liquidacion_iva()` (quedó por no tocar facturas mientras la terminal lo mueve). Reconciliar el gate FABS-INC1 en el tracker `.xlsx` cuando la terminal deje de escribirlo (se registró aquí para no chocar el binario). | — |
| 2026-08-11 | 2 | **Incremento 2 ARRANCADO.** CEO aprobó el diseño: D1 loop acotado (máx. 3 iter, temp 0.1, modelo por config), D2 abstención estricta (cifra sin evidencia ⇒ no se publica; 1 reintento y si no, se abstiene), D3 endpoint `POST /api/v1/cfo` tras el flag. Reconocimiento del repo hecho (catálogo de auditoría 60 eventos, `require_permission`, deps sin `anthropic`, calc de inc1). Escribiendo spec → plan → SDD. **Must-do primero:** blindar `iva_cuatrimestre` por periodicidad (§6). | spec en `docs/superpowers/specs/2026-08-11-fabs-inc2-*` |
| 2026-08-17 | 2 | **Incremento 2 CONSTRUIDO (SDD, 12 tareas + fixes, subagente+review por tarea).** `app/cfo/agente/`: modelos · tools de solo lectura · system prompt (el modelo nunca calcula, ni %) · **verificador cifra→evidencia** · cliente Anthropic lazy (`anthropic==0.100.0`) · loop acotado · servicio orquestador (verify/retry/abstención + auditoría fail-soft) · endpoint `POST /api/v1/cfo` doble barrera + capacidad `cfo:consultar`. Eventos `cfo.consulta`/`cfo.respuesta` (CR-CFO-1, catálogo 62→64). Guard IVA por periodicidad. `motor.py` cero diffs; flag `CFO_ENABLED` apagado ⇒ COMPAS byte-idéntico. **Suite backend 1009 passed / 0 failed.** | `feat/fabs-inc2` `4085944..1955cb9` |
| 2026-08-17 | 2 | **El verificador (control anti-alucinación) costó 2 rondas de fix + 1 en el review final** — el gate cazó defectos REALES: (a) el formato *wire* real de las tools es `NNN.dd` (2 decimales vía `money_str`), no el humano es-CO que asumió el plan → una cifra fabricada en dígitos pelados pasaba sin verificar; (b) los porcentajes (`25%`) se colaban. Cerrados: normalizador por semántica-de-separadores + `%` siempre huérfano. Es justo el valor del gate: cerrar huecos de fabricación ANTES de conectar el LLM. | re-reviews en `planning/phases/fabs-inc2/` |
| 2026-08-17 | 2 | **Review final whole-branch (opus): "Ready to merge — With fixes", cero Critical de código.** Propiedad anti-alucinación verificada end-to-end; money=Decimal; flag-off idéntico; auditoría fail-soft; RBAC (actor_id real, sin over-grant). **Deferral vinculante:** el verificador agrupa evidencia por *unidad*, no por *concepto* (caja e iva comparten COP) → documentado en código; OK con flag OFF, **MUST cerrarse antes de encender `CFO_ENABLED` (inc3)**. | review en `.superpowers/sdd/…` |
| 2026-08-17 | 2 | **Incremento 2 MERGEADO a main** (ff `06bdbab..7a8bd07`). Antes del push: merge de `origin/main` (10 commits paralelos: espejo agosto, PLAN-52, tornado, config NITs) → **limpio, sin conflictos** (archivos disjuntos; la terminal tocó `goldens/runner.py`, que inc2 no toca). **Suite completa post-merge: 1039 passed / 95 skipped / 0 failed.** Gate: **gate-waiver GO CEO 2026-08-17** (flag apagado ⇒ main byte-idéntico; riesgo mínimo); Kimi retroactivo pendiente con `PAQUETE.pdf`. Tracker `.xlsx`: reconciliar el gate FABS-INC2 cuando la terminal deje de escribirlo (se registra aquí para no chocar el binario, como inc1). | `7a8bd07` |
| — | 2→3 | **Precondiciones para encender el flag (inc3), NO ahora:** `ANTHROPIC_API_KEY` en Render + cerrar la verificación *concept-aware* (deferral vinculante) + respetar **O-1** (no encender en prod sin auditoría retroactiva). Canal Telegram = inc3. | — |
| 2026-08-17 | 3A | **inc3 partido en 2 piezas (GO CEO): Pieza A = verificación por concepto (prerrequisito), Pieza B = Telegram. Gate de diseño Kimi 9.3 GO.** Decisiones: allowlist admin para el vínculo · hilos con contexto (Pieza B) · cerrar la verificación por concepto ANTES de encender. | spec/plan `docs/superpowers/…-fabs-inc3a-…` |
| 2026-08-17 | 3A | **Pieza A CONSTRUIDA + REVISADA (SDD, 6 tareas + fix waves).** Cierra el deferral vinculante de inc2: el modelo ya NO ve `valor` (tools), cita `[[caja_hoy]]`/`[[runway]]`/`[[iva_cuatrimestre]]`; el verificador PROHÍBE cifras crudas + valida tokens (concepto disponible este turno); el servicio SUSTITUYE tras verificar (orden verify→sustituir, nunca re-verifica). D-1: IVA muestra "vence en N días" (server-bound), runway=duración. Mislabel caja/IVA imposible por construcción. Review final opus "Ready to merge — Yes", 0 Crit/Imp; RE_TOKEN compartido+tolerante a espacios. `motor.py` 0 diffs; flag apagado ⇒ COMPAS idéntico; **suite 1044 passed/0 failed.** | `feat/fabs-inc3a-concepto` `557d8ac..6de54ef` |
| 2026-08-17 | 3A | **Pieza A MERGEADA a main** (ff `61ded9c..6fc1e87`; origin/main no se había movido). Gate: **gate-waiver GO CEO 2026-08-17** (flag apagado ⇒ main byte-idéntico); Kimi de código retroactivo pendiente con `PAQUETE.pdf` (INC3A-I). El deferral vinculante de inc2 (verificación concept-aware) queda CERRADO. Tracker `.xlsx`: reconciliar cuando la terminal deje de escribirlo. | `6fc1e87` |
| — | 3B→ | **Siguiente: Pieza B (Telegram + piloto).** Precondiciones para ENCENDER el flag (no en la Pieza B necesariamente, sino en go-live): `ANTHROPIC_API_KEY` en Render + O-1 (no encender en prod sin auditoría retroactiva). Chat embebido en COMPAS = inc5. | — |
| 2026-08-22 | 3B | **Pieza B CONSTRUIDA (SDD, 7 tareas).** `app/cfo/telegram/`: Task1 config del canal + `servicio.consultar` acepta `historial`/expone `texto_crudo` (aditivo, default `None` ⇒ inc2 idéntico) · Task2 CR-CFO-2 eventos `cfo.vinculo_creado`/`cfo.vinculo_eliminado` (catálogo 64→66) · Task3 Documents `cfo_vinculos_telegram`/`cfo_hilos` + `repositorio.py` (única puerta de escritura, S1) · Task4 cliente Telegram saliente (httpx lazy) + fake · Task5 lógica de vínculos (con auditoría) + hilos (historial CRUDO con `[[tokens]]`, nunca valores; dedup por `update_id`) · Task6 webhook (fail-closed: secret inválido→403, sin bot token→503, no-vinculado rehúsa sin llamar al servicio) + endpoint admin de vínculos gateado con `require_permission('cfo:telegram_administrar')` (no `require_role` — H-1/regla 9) + registro en `main.py` tras el flag (doble barrera). Fix rounds incluidos: duplicado de vínculo traducido a excepción de dominio (`VinculoDuplicado`) en la frontera del repositorio para que un fallo real de auditoría no se disfrace de 409. | rama `feat/fabs-inc3b-telegram`, `33d807d..2d805a7` |
| 2026-08-22 | 3B | **Cierre Pieza B (Task 7).** `test_s1_aislamiento.py` extendido: `LOGICA` ahora incluye `cfo/telegram/` — escaneo **limpio, SIN exención** (verificado con grep: ningún archivo contiene `app.domain.*`, ni `motor`/`AsyncIOMotor`/`get_pymongo_collection`; solo Beanie/pymongo ODM — igual precedente que `goldens/modelo.py`, que tampoco los necesitó). `test_flag_off_rutas_ausentes` (ya escrito en Task 6) confirma que `create_app()` con `CFO_ENABLED` ausente NO registra `/api/v1/cfo/telegram/webhook` ni `/api/v1/cfo/telegram/vinculos`. `git diff f4d651b..HEAD` sobre `app/proyeccion/motor.py` y `app/presupuesto/motor.py`: **vacío (0 diffs)**. `grep "float(" app/cfo/telegram/`: sin resultados (el único `float` en el módulo es `timeout_s: float = 10.0` del cliente httpx en `cliente.py` — un timeout de red, no dinero). `ruff check app/cfo/`: limpio. **Suite completa: 1085 passed / 98 skipped / 0 failed** (los skips son `requires_real_mongo`, incl. los 3 de `test_repositorio.py` — sin `COMPAS_TEST_MONGO_URI` en este entorno). **NO MERGEADA a main:** pendiente gate Kimi (A-retro ≥9 + B-código ≥9) + GO CEO — gobernanza de inc3 (decisión CEO 2026-08-17); flag `CFO_ENABLED` sigue apagado ⇒ COMPAS/main byte-idéntico. | rama `feat/fabs-inc3b-telegram`, commit de cierre (S1 + este roadmap) |
| 2026-08-22 | 3B | **Gates Kimi de código: INC3A-I 9.5 GO (retroactivo, salda el waiver de Pieza A) + INC3B-I 9.5 GO; ambas condiciones de gobernanza cumplidas.** → **Pieza B MERGEADA a main** (`ad0aec4`), flag `CFO_ENABLED` apagado ⇒ COMPAS byte-idéntico. | paquetes `planning/phases/fabs/auditorias/INC3A-I/` y `INC3B-I/`; merge `ad0aec4` |
| 2026-08-22 | 3B | **N-1/N-2 de Kimi cerrados** (fast-follow no bloqueante de los gates arriba): N-1 = comentario obsoleto en `test_prompt.py` corregido (el token espaciado hoy SÍ se sustituye, vía `RE_TOKEN`, no se filtra crudo). N-2 = `RespuestaCFO.texto_crudo` ahora REQUERIDO (sin default `None`) + eliminado el fallback `resp.texto_crudo or resp.texto` en `webhook.py` — cierra el camino teórico de fuga hacia el texto sustituido en el hilo (TDD: test RED con `texto_crudo=""` reproduciendo la fuga → fix → GREEN). | rama `chore/fabs-inc3-nits-kimi`, commit `a40731e` |
| 2026-08-23 | 4 | **Rebanada 1 ARRANCADA — "el caso de la bodega" (what-if de escenarios).** Brainstorming con el CEO → spec de diseño aprobado (gate crítico: toca cálculo de plata que el CEO usa para decidir) → plan de 9 tareas TDD. Alcance: impacto de un ajuste hipotético recurrente sobre la proyección, mes de quiebre del umbral, y solver de unidades extra/mes para no cruzarlo — todo sobre el motor real (`motor.py` intocable), el modelo nunca calcula. Fuera de alcance (fast-follow): escenarios multi-variable, anclar a la caja real de hoy (§7 del spec), persistir escenarios con nombre. Rama `feat/fabs-inc4-escenarios` (desde main `2511398`). | spec `docs/superpowers/specs/2026-08-23-fabs-inc4-cerebro-analitico-escenarios-design.md` (`6a8d64a`), plan `docs/superpowers/plans/2026-08-23-fabs-inc4-cerebro-analitico-escenarios.md` (`1cea353`) |
| 2026-08-26 | 4 | **Rebanada 1 CONSTRUIDA (SDD, 9 tareas + fix rounds, subagente+review por tarea).** `proyeccion/solver_unidades.py`: solver de unidades nuevo (bisección ENTERA acotada a `[0, cap_unidades]`, corre el pipeline COMPLETO motor→E1→D2 por candidato N, no solo el motor paramétrico — Fix round del Task 6 tras hallazgo de revisión Opus: con la base motor-only, el solver podía SUBESTIMAR el umbral frente a `impacto_escenario`). `app/cfo/calc/escenario.py`: dos calcs nuevas — `impacto_escenario` (envuelve `proyectar_impactos`, evidencia honesta con ancla de horizonte, `piso_con` trae el mes de quiebre en su `ref`) y `motos_para_evitar_umbral` (envuelve el solver vía `proyeccion.service.fabrica_proyectar_unidades`, respetando el aislamiento S1: `cfo/calc` no importa tipos de dominio). `app/cfo/agente/tools.py`+`conceptos.py`+`verificador.py`: tools parametrizadas (primera vez; `input_schema` estricto, `naturaleza` validada, `monto` string→Decimal), `ejecutar_tool` devuelve `list[ResultadoCFO]` (una tool puede nombrar VARIOS conceptos en un turno), nueva unidad citable `"unidades"` (formatea "N motos"), y el verificador cierra el hueco de conteo crudo (`_RE_UNIDADES`: "12 motos" sin token `[[unidades_extra]]` se rechaza igual que un monto crudo). System prompt extendido con las reglas de escenario (regla 1/2 sin cambios, solo alcance). **Cierre (Task 9): test end-to-end** (`tests/cfo/agente/test_servicio.py`) prueba con `ClienteFake` que el modelo pide las dos tools EN EL MISMO turno, cita 4 tokens (`[[piso_con]]`/`[[impacto_mensual]]`/`[[unidades_extra]]`/`[[piso_con_unidades]]`) y el texto publicado trae los valores sustituidos, nunca el token crudo; y que un intento de escribir el conteo crudo ("12 motos") dispara el reintento correctivo y, si reincide, abstención `motivo="verificacion"` (D-3: un solo reintento, jamás loop) — la garantía anti-alucinación probada de punta a punta con las tools nuevas. **Golden nuevo** (`tests/cfo/test_escenario_golden.py`): escenario de referencia fijo ("bodega $20M/mes desde septiembre") con los 5 números conocidos "al peso" (piso_sin $100M, piso_con $40M con quiebre en 2026-11, impacto_mensual $20M, y — corriendo el solver REAL, no fakeado, sobre una proyección analítica en forma cerrada — unidades_extra=12, piso_con_unidades $82M, matemática recalculada a mano en el docstring del test, no re-derivada con la fórmula del código bajo prueba). **Regresión completa: `python -m pytest -q` → 1264 passed / 98 skipped / 0 failed** (los skips son `requires_real_mongo`, sin `COMPAS_TEST_MONGO_URI` en este entorno). `ruff check app/cfo/ app/proyeccion/solver_unidades.py` limpio. `grep "float("` en `app/cfo/calc/escenario.py`/`app/proyeccion/solver_unidades.py`: sin resultados (dinero=Decimal en todo el slice). `git diff 2511398..HEAD -- app/proyeccion/motor.py app/presupuesto/motor.py`: **vacío (0 diffs)** — motor intocable en toda la rama. **MERGEADA a main (`31684e6`) y EN VIVO (2026-08-26):** el CEO autorizó soltar a producción (Kimi fuera ~semanas ⇒ modo gate-waiver de aquí en adelante). Smoke del CEO por Telegram **SANO**: bodega $10M/mes desde octubre → impacto $10M, piso SIN $121.199.133, piso CON $71.199.133 **idéntico desde `proyectar_impactos` y desde el solver de unidades ⇒ reconcilian** (el fix de fidelidad funciona en vivo), 0 motos extra (no cruza el umbral). Gate: **gate-waiver GO CEO 2026-08-23** + **Kimi diseño+código RETROACTIVOS PENDIENTES (Kimi no disponible ~semanas) — NUNCA simulado**; paquete `INC4-I` preparado para subir cuando Kimi vuelva. Nits cosméticos (fast-follow): "0 motos motos" (doble sustantivo) y lista de "Cifras" duplicada. Tracker `.xlsx`: no se toca (otra sesión en paralelo); este roadmap es el registro sustituto. | rama `feat/fabs-inc4-escenarios` → main `2511398..31684e6` |
| 2026-08-26 | 4 | **Rebanada 2 ARRANCADA — what-if de palancas de crédito (plazo/cuota inicial/cuota semanal).** Spec de diseño escrito y aprobado por el CEO (v1.1) → plan de 5 tareas TDD. Mismo patrón que rebanada 1: reusa `_resultado_con` (motor→E1→D2) con un override de `ModeloMoto` (`model_copy`) en vez de un ajuste externo; `motor.py` intocable; `cfo/calc` sin importar `app.domain.*` (S1). | spec `docs/superpowers/specs/2026-08-26-fabs-inc4-rebanada2-palancas-design.md` (`fc39e1a`), plan `docs/superpowers/plans/2026-08-26-fabs-inc4-rebanada2-palancas.md` (`9179e9b`), task-briefs en `.superpowers/sdd/2026-08-26-fabs-inc4-rebanada2-palancas/` |
| 2026-08-29 | 4 | **Rebanada 2 CONSTRUIDA (SDD, 5 tareas, subagente+review por tarea).** `app/proyeccion/service.py`: `impacto_palanca_raw` (Task 1) — re-corre el pipeline COMPLETO dos veces (base vs. con la palanca aplicada a uno o "todos" los modelos, vía `ModeloMoto.model_copy(update={palanca: valor})`), devuelve `PalancaImpacto` (piso_sin/piso_con/mes_quiebre/impacto) en tipos planos Decimal/str; reusa `_cargar_config_vigente` (fix DRY del propio Task 1). `app/cfo/calc/palanca.py`: `impacto_palanca` envuelve `impacto_palanca_raw` en 3 `ResultadoCFO` (piso_sin/piso_con/impacto), abstención honesta (`disponible=False`) si la palanca/modelo/valor es inválido o no hay config vigente — mismo molde que `escenario.impacto_escenario`. `app/cfo/agente/tools.py`: tool `simular_palanca` (enum de palanca ∈ {plazo_semanas, cuota_inicial, cuota_semanal}, modelo ∈ {Raider, Apache, Sport, todos}, `nuevo_valor` string→Decimal validado finito) registrada en `DISPATCH`/`TOOLS_SCHEMA` sin tocar las 5 tools previas. `app/cfo/agente/prompt.py`: bloque nuevo que enseña `simular_palanca` y exige citar los 3 tokens (`[[piso_sin]]`/`[[piso_con]]`/`[[impacto]]`) sin auto-calcular la resta. **Cierre (Task 5): e2e** (`tests/cfo/agente/test_servicio.py`) — a diferencia de los e2e de escenario (que mockean `loop.ejecutar_tool`), este deja correr el DISPATCH/tools.py REAL y solo fakea `app.cfo.calc.palanca.impacto_palanca`, probando también el wiring tool→calc; confirma sustitución de los 3 tokens y, en el caso de cifra cruda, reintento correctivo (D-3) → abstención `motivo="verificacion"`. **Golden nuevo** (`tests/cfo/test_palanca_golden.py`): corre `impacto_palanca_raw` REAL (motor→E1→D2 completos) sobre una BD mongomock vacía, fakeando solo la carga de `ParametrosProyeccion`/`ModeloMoto` — palanca de referencia plazo_semanas 12→78 (base corta a propósito: con un plazo base ≥~17 semanas frente al horizonte de 12 meses el cambio resultó DEGENERADO — piso_sin=piso_con, confirmado experimentalmente antes de fijar el escenario — así que se documenta el mecanismo y se elige un caso donde el efecto es real y grande); candado de DIRECCIÓN (`piso_con ≥ piso_sin`, plazo más largo con la misma cuota semanal nunca reduce el recaudo dentro del horizonte) + identidad de reconciliación `impacto == piso_con - piso_sin`, además de los 4 valores congelados. **Regresión: `pytest tests/cfo -q` → 176 passed / 3 skipped** (skips = Mongo real, sin relación); `pytest tests/proyeccion -q` → 10 passed; `test_s1_aislamiento.py` verde. `ruff check`/`ruff format --check` limpios en los archivos tocados. `git diff fb68860..HEAD -- app/proyeccion/motor.py app/presupuesto/motor.py`: **vacío (0 diffs)** (`fb68860` = cierre de Task 4, el commit inmediatamente anterior a este Task 5; el rango completo de la rama `41cb535..HEAD` sí toca `motor.py`, pero es el commit `1001a42` de COMPAS 2.0 Fase 0/RF-F3 — ajeno a esta rebanada, ya en la rama antes de que Task 1 arrancara). `grep "float(" app/cfo/calc/palanca.py`: sin resultados. **CONSTRUIDA, sin mergear a main:** gate-waiver GO CEO 2026-08-29 (Kimi sigue ausente ~semanas, retroactivo pendiente — NUNCA simulado, memoria `kimi-no-disponible-semanas`); a diferencia de rebanada 1, el flag `CFO_ENABLED` YA ESTÁ ENCENDIDO en prod desde que rebanada 1 quedó en vivo, así que mergear esta rama activa `simular_palanca` de inmediato para el piloto de Telegram — decisión de release (cuándo mergear) queda con el CEO, no es un gate-waiver automático como en incrementos anteriores. Paquete Kimi `INC4-R2` (diff embebido) lo arma el controlador post-SDD. Tracker `.xlsx`: no se toca (este roadmap es el registro sustituto, regla del CEO). **Siguiente: mezclar (mix) de modelos → rebanada 4** (`participacion_mix` como palanca what-if). | rama `feat/fabs-inc4-rebanada2-palancas`, commits `f6f47a8..fb68860` (tareas 1-4) + commit de cierre (Task 5, este roadmap) |

## 5. Estado de datos / decisiones abiertas del CEO

- **Alegra:** CERO en esta fase (revisión 2027 si se requiere).
- **Fuera de alcance permanente:** CXC socios, interés presuntivo, devengado/P&L, labores contables (COMPAS/FABS NO son ERP).
- **Fórmulas faltantes** (si las hubiera): se construyen EN COMPAS (aditivas, motor intocable), FABS las consume (D5).
- **Pendientes del CEO:** rotación del token Alegra filtrado en SISMO-V2 (higiene de seguridad); GO al CR-PTS6F (cartera de apertura); merge de `feat/fabs-inc1` a main tras el gate.

## 6. Refinamientos conocidos (para incrementos siguientes)

- **`caja_hoy` — semántica de anclaje (inc2):** hoy corre `caja_diaria` desde el `vigente_desde` de los parámetros vigentes. Como esos parámetros se re-guardan seguido (vigente_desde = fecha reciente) y no hay movimientos cargados en esa ventana, en PROD devolvió la `caja_inicial` cruda (704.722.003) con `ref="sin-movimientos"`, no la caja corrida real. Refinar en inc2: anclar desde el último mes CERRADO (o desde el primer movimiento del mes en curso) para reflejar la caja real a la fecha de corte. La abstención honesta y la evidencia ya funcionan; es un ajuste de fuente, no de contrato.
- **`proximo_pago` lee reloj dentro de módulo "compute-only"** (`app/iva/liquidacion.py`, preexistente, reubicado en el refactor DRY): el reviewer final dictaminó que `dias` (días al vencimiento DIAN) es intrínsecamente relativo al reloj → `today_bogota()` es semánticamente correcto ahí; opcional pasar "hoy" como parámetro para determinismo pleno. NO bloquea.
- **`iva_cuatrimestre` asume periodicidad cuatrimestral** (`app/cfo/calc/iva.py` `_periodo_vigente_idx`, `(month-1)//4`): ignora `data["periodicidad"]` que ya trae. Hoy exposición CERO (RODDOS cuatrimestral, flag apagado, sin consumidor). **MUST-DO al inicio de inc2, ANTES de que el LLM consuma esta cifra:** derivar el índice de `data["periodicidad"]` o fail-closed (abstenerse) si `periodicidad != "cuatrimestral"` — para no publicar jamás una cifra/evidencia equivocada (regla #1).
- **`upsert_golden` es insert-si-ausente, no upsert real** (`app/cfo/datos/repositorios.py`): idempotente por `(concepto, nota)` sin índice único; si cambia un valor con la misma `nota`, re-sembrar no lo actualiza. Aceptable para siembra de una sola vez; revisar cuando Fabián cargue el set 240+60.

---
*Creado 2026-08-10. Este archivo se actualiza al cerrar cada pieza (no al final del incremento).*
